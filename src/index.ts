import { addition } from "./functions/addition";
import {sort} from "./functions/sort";
const output: number = addition(0); // 6
const sortedOutput: number[] = await sort(23,32,3,54,56,5,76,76); // [1, 2, 3]
console.log(output);